# Flask session signing key. Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me

# OpenAI API key used by the AgroAssist chatbot on /chat
OPENAI_API_KEY=sk-your-key-here
